Object.defineProperty( window, 'root', { get: () => document.documentElement } )

// document.extends( Document );
if( document )
{
// 	let klass = document.createClass();
// 	document.extends(klass);
	document.Document();
}


// Jilex.boot();

})( window, document );
